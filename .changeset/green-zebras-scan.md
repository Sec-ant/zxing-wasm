---
"zxing-wasm": patch
---

Update ZXing-C++ with the latest decoding fixes, expose `rotation` as the canonical read-result property while preserving `orientation` as deprecated, and refresh the development toolchain.
