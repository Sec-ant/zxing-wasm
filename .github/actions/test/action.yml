name: Test
description: Run tests

runs:
  using: composite
  steps:
    - name: Build WASM
      shell: bash
      run: pnpm build:wasm

    - name: Run Tests
      shell: bash
      run: pnpm test --exclude ".*/**/*"
